@AGENTS.md

When writing or editing documentation copy, use the `write-docs` skill (`.claude/skills/write-docs/SKILL.md`). Match the voice and structure of the entries already in the file, keep feature and release entries to one tight paragraph, and state the benefit as a concrete contrast (`generated gradients instead of image tiles`) rather than describing the mechanism.
