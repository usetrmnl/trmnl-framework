<!-- ZERO.md — zero's setup file for this project, one per repository,
     the way a CLAUDE.md is. It holds one thing so far: the vocabulary
     this project's voice memos are transcribed and cleaned up against.
     One term per line: "- Term — notes about how transcription mangles it".
     The term is fed to speech recognition; the whole line guides cleanup.
     Meant to be committed, so the project's words travel with the repo. -->
- trmnl-framework — this project's name; restore it exactly as written here if the transcription mangles it.
- TRMNL — heard as "terminal"; vowel-less brand spelling
- terminalize() — heard as "terminal eyes" or "terminalise"
- TRMNLPaint — heard as "terminal paint"
- TRMNLCharts — heard as "terminal charts"
- Sass — heard as "sass", "SAS", or "sass" vs "SCSS"
- dartsass-rails — heard as "dart sass rails" or "dark sass"
- propshaft — heard as "prop shaft" as two words
- importmap — heard as "import map" as two words
- cssnano — heard as "CSS nano" or "CSS nano" split
- postcss-rename — heard as "post CSS rename"
- terser — heard as "tercer", "terse-er", or "Tercer"
- rulediff — heard as "rule diff" as two words
- Chartkick — heard as "chart kick" or "Chartkit"
- Highcharts — heard as "high charts" as two words
- Playwright — heard as "play write" or "playwrite"
- Chromium — heard as "chromium" vs "chrome-ium" spelling drift
- BlockKie — heard as "blocky"; double-K capitalisation lost
- opentype.js — heard as "open type JS" split into words
- rbenv — heard as "R-B-env" or "arbenv"
- jsDelivr — heard as "JS deliver"; missing "e"
- OpenAI — misheard as: open AIs (suggested)
- OpenStreetMap — misheard as: open street maps, open switch maps (suggested)
- Strava — misheard as: travel; "Strava plugin" heard as "Strava plugger" (suggested)
- TRMNLMaps — misheard as: determining maps (suggested)
- Inter — misheard as: intern, enter (suggested)
