<%# "Select" glyph: a checkbox with a check. Used as the mobile/tablet affordance for
    the bulk select-mode toggle (the desktop button keeps a trailing ellipsis instead). %>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor"
     stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"
     class="flex-shrink-0 <%= local_assigns[:classes] || 'h-5 w-5' %>">
  <rect x="3" y="3" width="18" height="18" rx="5"/>
  <path d="m8 12.5 2.5 2.5L16 9"/>
</svg>
